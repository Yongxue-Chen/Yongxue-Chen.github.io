const{test:test,expect:expect}=require("@playwright/test");test("homepage has clear identity and prose research summary",async({page:t})=>{await t.goto("/"),await expect(t.locator(".post-header .post-title")).toHaveText("Yongxue Chen"),await expect(t.locator(".home-stat")).toHaveCount(0),await expect(t.locator("article .lang-en").first()).toContainText("14 peer-reviewed journal articles"),await expect(t.locator("article .lang-en").first()).toContainText("5 granted Chinese invention patents"),await expect(t.locator(".profile img")).toHaveAttribute("alt",/Yongxue Chen|prof_pic/i),await expect(t.locator(".home-education .edu-item")).toHaveCount(3),await expect(t.locator(".home-education .edu-school")).toHaveCount(6),await expect(t.locator(".home-section h3 .lang-en").nth(1)).toHaveText("Selected Publications"),await expect(t.locator(".home-section h3 .lang-en").nth(2)).toHaveText("Selected Awards"),await expect(t.locator(".home-intro.lang-en")).toContainText("robotics"),await expect(t.locator(".home-intro.lang-en")).toContainText("advanced manufacturing"),await expect(t.locator(".home-intro.lang-en")).toContainText("hybrid additive-subtractive manufacturing");const e=await t.locator(".home-intro.lang-en strong").first().evaluate(t=>getComputedStyle(t));expect(Number(e.fontWeight)).toBeGreaterThanOrEqual(700),expect(e.backgroundImage).not.toBe("none")}),test("publications use one consistent collapsible layout",async({page:t})=>{await t.goto("/publications/"),await expect(t.locator(".post-header .desc")).toHaveCount(0);const e=t.locator(".publications ol.bibliography li");await expect(e).toHaveCount(14);const a=t.locator(".publications .abstract.hidden");await expect(a).toHaveCount(13);for(const t of await a.all())await expect(t).toBeHidden();await expect(t.locator(".links a.abstract.btn")).toHaveCount(13),await expect(t.locator(".links a.abstract.btn .lang-en").first()).toHaveText("Abstract");const o=await t.locator(".publications img.preview").first().evaluate(t=>{const e=t.getBoundingClientRect();return e.width/e.height});expect(o).toBeCloseTo(4/3,2);const c=await e.evaluateAll(t=>t.map(t=>{const e=getComputedStyle(t);return[e.paddingTop,e.paddingBottom,e.borderBottomStyle,e.borderBottomWidth]}));expect(new Set(c.map(t=>t.join("|"))).size).toBe(1)}),test("projects use the requested card rows and embedded videos",async({page:t})=>{await t.goto("/projects/"),await expect(t.locator(".post-header .desc")).toHaveCount(0),await expect(t.locator(".project-section")).toHaveCount(4),await expect(t.locator(".project-card")).toHaveCount(8),await expect(t.locator(".project-video-trigger")).toHaveCount(2),await expect(t.locator(".project-media iframe")).toHaveCount(0),await t.locator(".project-video-trigger").first().click(),await expect(t.locator(".project-media iframe")).toHaveCount(1),await expect(t.locator(".project-meta")).toHaveCount(8),await expect(t.locator(".project-card").first().locator(".project-meta .lang-en")).toHaveText("ACM TOG (Conditionally Accepted)");const e=await t.locator(".project-media").first().evaluate(t=>{const e=t.getBoundingClientRect();return e.width/e.height});expect(e).toBeCloseTo(4/3,2);const a=await t.locator(".post-header").evaluate(t=>getComputedStyle(t).marginBottom);expect(a).toBe("48px");const o=await t.locator(".project-grid").evaluateAll(t=>t.map(t=>getComputedStyle(t).gridTemplateColumns.split(" ").length));expect(o).toEqual([2,3,2,1]);const c=await t.locator(".project-grid--single").evaluate(t=>t.querySelector(".project-card").getBoundingClientRect().width/t.getBoundingClientRect().width);expect(c).toBeGreaterThan(.48),expect(c).toBeLessThan(.51);const i=await t.locator(".project-section-heading").first().evaluate(t=>getComputedStyle(t));expect(i.borderLeftWidth).toBe("4px"),expect(i.backgroundImage).not.toBe("none")}),test("CV uses a sticky desktop index and places research experience after education",async({page:t})=>{await t.setViewportSize({width:1280,height:900}),await t.goto("/cv/"),await expect(t.locator(".post-header .desc")).toHaveCount(0);const e=await t.locator(".cv-sidebar").evaluate(t=>getComputedStyle(t));expect(e.position).toBe("sticky"),await expect(t.locator(".cv-contact-links a")).toHaveCount(3),await expect(t.locator(".cv-contact-links a").nth(0)).toHaveAttribute("href","mailto:chandler.yx.chen@gmail.com"),await expect(t.locator(".cv-contact-links a").nth(1)).toHaveAttribute("href",/scholar\.google\.com/),await expect(t.locator(".cv-contact-links a").nth(2)).toHaveAttribute("href",/linkedin\.com/),expect(await t.locator(".cv-contact").evaluate(t=>getComputedStyle(t).display)).toBe("block"),expect(await t.locator(".cv-nav-title").evaluate(t=>getComputedStyle(t).display)).toBe("block");const a=await t.locator(".cv-nav").evaluate(t=>getComputedStyle(t));expect(a.flexDirection).toBe("column"),expect(a.flexWrap).toBe("nowrap"),expect(a.overflowX).toBe("visible");const o=await t.locator(".cv-nav a").first().evaluate(t=>getComputedStyle(t));expect(o.backgroundColor).toBe("rgba(0, 0, 0, 0)"),expect(o.borderTopWidth).toBe("0px");const c=await t.locator("h2.cv-heading").first().evaluate(t=>getComputedStyle(t));expect(c.borderLeftWidth).toBe("4px"),expect(c.backgroundImage).not.toBe("none"),await expect(t.locator("h2.cv-heading .lang-en").nth(0)).toHaveText("Education"),await expect(t.locator("h2.cv-heading .lang-en").nth(1)).toHaveText("Research Experience"),await expect(t.locator(".cv-section-note")).toHaveCount(2),await expect(t.locator("#research-experience + .cv-section .cv-section-note .lang-en").first()).toHaveText("Selected research themes."),await expect(t.locator("#journal-articles + .cv-section .cv-section-note .lang-en").first()).toHaveText("Complete publication list."),await expect(t.locator("h2#selected-awards")).toHaveCount(1),await expect(t.locator('.cv-section-note a[href="/publications/"]')).toHaveCount(1),await expect(t.locator('.cv-section-note a[href="/projects/"]')).toHaveCount(1);const i=await t.locator(".cv-section-note a").first().evaluate(t=>getComputedStyle(t));expect(i.backgroundColor).not.toBe("rgba(0, 0, 0, 0)"),expect(i.color).toBe("rgb(255, 255, 255)");const n=await t.locator(".cv-section-note a span").first().evaluate(t=>getComputedStyle(t).color);expect(n).toBe("rgb(255, 255, 255)"),await expect(t.locator("#journal-articles + .cv-section .cv-entry")).toHaveCount(14),await expect(t.locator("#research-experience + .cv-section .cv-entry")).toHaveCount(4),await expect(t.locator("#research-experience + .cv-section .cv-entry").nth(0).locator("ul.lang-en li")).toHaveCount(2),await expect(t.locator("#research-experience + .cv-section .cv-entry").nth(1).locator("ul.lang-en li")).toHaveCount(3),await expect(t.locator("#research-experience + .cv-section .cv-entry").nth(2).locator("ul.lang-en li")).toHaveCount(2),await expect(t.locator("#research-experience + .cv-section .cv-entry").nth(3).locator("ul.lang-en li")).toHaveCount(1),await expect(t.locator(".cv-nav a.active")).toHaveAttribute("href","#education"),await t.locator("#research-experience").evaluate(t=>t.scrollIntoView()),await expect(t.locator(".cv-nav a.active")).toHaveAttribute("href","#research-experience"),await t.setViewportSize({width:390,height:844}),expect(await t.locator(".cv-contact").evaluate(t=>getComputedStyle(t).display)).toBe("none"),expect(await t.locator(".cv-nav-title").evaluate(t=>getComputedStyle(t).display)).toBe("none");const r=await t.locator(".cv-sidebar").evaluate(t=>getComputedStyle(t)),l=await t.locator(".cv-nav").evaluate(t=>getComputedStyle(t));expect(r.position).toBe("static"),expect(l.flexWrap).toBe("wrap")}),test("language selection updates document semantics and navigation",async({page:t})=>{await t.goto("/publications/"),await t.getByRole("button",{name:"\u4e2d\u6587"}).click(),await expect(t.locator("html")).toHaveAttribute("lang","zh-CN"),await expect(t.locator(".post-title")).toHaveText("\u8bba\u6587"),await expect(t.locator(".links a.abstract.btn .lang-zh").first()).toHaveText("\u6458\u8981"),await expect(t.locator('a.nav-link[href="/projects/"]')).toContainText("\u9879\u76ee"),await expect(t.locator('a.nav-link[href="/cv/"]')).toContainText("\u7b80\u5386"),await expect(t.locator('a.nav-link[href="/life/"]')).toContainText("\u751f\u6d3b")}),test("primary pages do not overflow a mobile viewport",async({page:t})=>{await t.setViewportSize({width:390,height:844});for(const e of["/","/projects/","/publications/","/cv/","/life/"]){await t.goto(e);const a=await t.evaluate(()=>document.documentElement.scrollWidth-document.documentElement.clientWidth);expect(a).toBeLessThanOrEqual(1)}}),test("Life page provides four simple, responsive personal photo slots",async({page:t})=>{await t.setViewportSize({width:1280,height:900}),await t.goto("/life/"),await expect(t.locator(".post-header .desc")).toHaveCount(0),await expect(t.locator(".life-section")).toHaveCount(0),await expect(t.locator(".life-photo")).toHaveCount(4),await expect(t.locator(".life-placeholder")).toHaveCount(0),await expect(t.locator(".life-photo img")).toHaveCount(4),await expect(t.locator(".life-photo--hiking")).toHaveCount(2),await expect(t.locator(".life-photo--diving")).toHaveCount(1),await expect(t.locator(".life-photo--drumming")).toHaveCount(1),await expect(t.locator(".life-page figcaption")).toHaveCount(0);const e=await t.locator(".life-grid").evaluate(t=>getComputedStyle(t).gridTemplateColumns.split(" ").length);expect(e).toBe(12);const a=await t.locator(".life-photo--hiking").evaluateAll(t=>t.map(t=>t.getBoundingClientRect().top));expect(a[1]-a[0]).toBeGreaterThan(30),await t.setViewportSize({width:390,height:844});const o=await t.locator(".life-grid").evaluate(t=>getComputedStyle(t).gridTemplateColumns.split(" ").length);expect(o).toBe(1)});